{
	"name": "@arcmantle/forge-docs-client",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "tsc --noEmit && vite build",
		"check": "tsc --noEmit",
		"test:e2e": "pnpm run build && pnpm exec playwright test",
		"test:e2e:headed": "pnpm run build && pnpm exec playwright test --headed"
	},
	"dependencies": {
		"lit": "^3.3.1",
		"lit-html": "^3.3.1"
	},
	"devDependencies": {
		"@arcmantle/eslint-config": "file:../../tooling/eslint-config",
		"@arcmantle/tsconfig": "file:../../tooling/tsconfig-config",
		"@playwright/test": "^1.58.2",
		"@types/node": "latest",
		"typescript": "latest",
		"vite": "catalog:forge"
	}
}