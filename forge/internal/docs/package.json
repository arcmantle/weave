{
	"name": "@arcmantle/forge-docs-client",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun run check && bun run build.ts",
		"check": "bun x tsc --noEmit",
		"test:e2e": "bun run build && bunx playwright test",
		"test:e2e:headed": "bun run build && bunx playwright test --headed"
	},
	"dependencies": {
		"lit": "^3.3.1",
		"lit-html": "^3.3.1"
	},
	"devDependencies": {
		"@arcmantle/eslint-config": "file:../../../tooling/eslint-config",
		"@arcmantle/tsconfig": "file:../../../tooling/tsconfig-config",
		"@playwright/test": "^1.58.2",
		"@types/bun": "latest",
		"@types/node": "latest",
		"typescript": "latest"
	}
}