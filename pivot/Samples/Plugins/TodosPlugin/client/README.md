# TodosPlugin — Client

This plugin does not currently include a client-side component.

A future client implementation could provide:

- A Kanban board for managing todos
- Drag-and-drop task assignment to users
- Real-time updates via SignalR integration
