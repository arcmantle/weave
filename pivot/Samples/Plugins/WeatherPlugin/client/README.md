# WeatherPlugin — Client

This plugin does not currently include a client-side component.

A future client implementation could provide:

- A weather dashboard widget
- Forecast cards with temperature visualization
- Integration with the widget grid system
