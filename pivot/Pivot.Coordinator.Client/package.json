{
	"name": "@arcmantle/pivot-coordinator-client",
	"version": "1.0.0",
	"description": "Pivot Coordinator Client - Admin interface for managing Pivot systems",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"test": "vitest",
		"test:coverage": "vitest --coverage",
		"test:ui": "vitest --ui"
	},
	"dependencies": {
		"@arcmantle/pivot-client-auth": "1.0.0",
		"@arcmantle/pivot-client-router": "1.0.0",
		"@lit/context": "catalog:pivot",
		"lit": "catalog:pivot",
		"tslib": "catalog:pivot"
	},
	"devDependencies": {
		"@arcmantle/tsconfig": "workspace:^",
		"happy-dom": "catalog:pivot",
		"typescript": "catalog:pivot",
		"vite": "catalog:pivot",
		"vitest": "catalog:pivot"
	}
}
