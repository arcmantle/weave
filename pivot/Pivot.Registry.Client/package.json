{
	"name": "@arcmantle/pivot-registry-client",
	"version": "1.0.0",
	"description": "Pivot Registry Client - Web UI for managing plugin registry",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"test": "vitest",
		"test:coverage": "vitest --coverage",
		"test:ui": "vitest --ui"
	},
	"dependencies": {
		"@arcmantle/pivot-client-auth": "1.0.0",
		"@arcmantle/pivot-client-router": "1.0.0",
		"@lit/context": "catalog:pivot",
		"lit": "catalog:pivot",
		"marked": "catalog:pivot",
		"marked-shiki": "catalog:pivot",
		"shiki": "catalog:pivot",
		"tslib": "catalog:pivot"
	},
	"devDependencies": {
		"@arcmantle/tsconfig": "workspace:^",
		"happy-dom": "catalog:pivot",
		"typescript": "catalog:pivot",
		"vite": "catalog:pivot",
		"vitest": "catalog:pivot"
	}
}
