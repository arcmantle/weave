// Login page entry point.
// This is a separate Vite entry that only loads the lightweight login component,
// keeping the main app bundle out of the login page's critical path.
import '@arcmantle/pivot-client-auth';
